


<div class="row">

  <% @users.each do |user| %>
  <div class="col-sm-3">
    <div class="card mb-3">
      <%# <%= image_tag user_demo_image, class: 'card-img-top', alt: 'Card image cap' %>
      <div class="card-body">
        <h5 class="card-title"><%= user.name %></h5>
        <%= link_to "計測する！", user_path(user), class: "btn btn-border" %>
      </div>
    </div>
  </div>  
  <% end %>
</div>
