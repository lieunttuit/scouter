<p>
  <i class="fa-solid fa-chart-simple fa-3x mt-5 text-danger"></i>
</p>
<h3 class="text-danger mb-4">- <%= @user.name %>さんの印象は？ -</h3>

<div class="bg-lightred py-3">
  <p class="lead">Total</p>
  <div class="display-3 text-danger">
    <%= impression_total %>
  </div><small>/100</small>
  <p class="text-danger lead font-weight-bold mt-2"><%= impression_comment(@user.sex) %></p>
  <p class="mt-2 lead text-secondary">（<%= @evaluations.count %>人に評価されました！）</p>
</div>

<p>
  <i class="fa-solid fa-user-group fa-3x  mt-5 text-info"></i>
</p>
<h3 class="text-info py-3">- 世代別支持率 -</h3>
<div class="bg-lightblue py-3">
  <p class="lead">10代：<span class="text-info"><%= impression_each_generation(0) %></span><%= heart_each_generation(0) %></p>
  <p class="lead">20代：<span class="text-info"><%= impression_each_generation(1) %></span><%= heart_each_generation(1) %></p>
  <p class="lead">30代：<span class="text-info"><%= impression_each_generation(2) %></span><%= heart_each_generation(2) %></p>
  <p class="lead">40代：<span class="text-info"><%= impression_each_generation(3) %></span><%= heart_each_generation(3) %></p>
  <p class="lead">50代：<span class="text-info"><%= impression_each_generation(4) %></span><%= heart_each_generation(4) %></p>
  <p class="lead">60代以上：<span class="text-info"><%= impression_each_generation(5) %></span><%= heart_each_generation(5) %></p>
</div>




