<p>
  <i class="fa-solid fa-chart-simple fa-3x mt-5 text-brown"></i>
</p>
<h3 class="text-brown mb-4">- <%= @user.name %>さんの印象は？ -</h3>

<div class="bg-white rounded-corner border py-5 mx-3">
  <p class="lead font-weight-bold">Total</p>
  <div class="display-4 text-danger">
    <%= impression_total %>
  </div><small>/100</small>
  <p class="text-danger lead font-weight-bold mt-2"><%= impression_comment(@user.sex) %></p>
  <p class="mt-2 lead text-secondary">（<%= @evaluations.count %>人に評価されました！）</p>
</div>

<p>
  <i class="fa-solid fa-user-group fa-3x  mt-5 text-brown"></i>
</p>
<h3 class="text-brown py-3">- 世代別支持率 -</h3>
<div class="bg-white rounded-corner border py-5 mx-3">
  <p class="lead">10代：<span class="text-info"><%= impression_graph(0) %></span><span class="text-info pl-2">（<%= impression_each_generation(0) %>）</span></p>
  <p class="lead">20代：<span class="text-info"><%= impression_graph(1) %></span><span class="text-info pl-2">（<%= impression_each_generation(1) %>）</span></p>
  <p class="lead">30代：<span class="text-info"><%= impression_graph(2) %></span><span class="text-info pl-2">（<%= impression_each_generation(2) %>）</span></p>
  <p class="lead">40代：<span class="text-info"><%= impression_graph(3) %></span><span class="text-info pl-2">（<%= impression_each_generation(3) %>）</span></p>
  <p class="lead">50代：<span class="text-info"><%= impression_graph(4) %></span><span class="text-info pl-2">（<%= impression_each_generation(4) %>）</span></p>
  <p class="lead">60代以上：<span class="text-info"><%= impression_graph(5) %></span><span class="text-info pl-2">（<%= impression_each_generation(5) %>）</span></p>
</div>




